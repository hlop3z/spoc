"""Components of kind `$kind` for the $app_name app."""

from framework import $decorator


@$decorator
class Example:
    """
    A starter component.

    PEP 8 class names convert to their snake_case identifier automatically, so
    this registers as `$kind:$app_name.example`. Rename it, add more beside it,
    or delete this file if the app does not need this kind.
    """
