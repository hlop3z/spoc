"""The entire framework definition for $project_name: one declaration."""

import spoc

framework = spoc.Framework($kinds_args)

# One decorator per declared kind. Apps import these to declare components.
$kind_decorators
