[project]
name = "spoc"
version = "0.1.0"
description = ""
authors = [
    {name = "hlop3z", email = "23062270+hlop3z@users.noreply.github.com"},
]
dependencies = []
requires-python = ">=3.10"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-pep517>=1.0.0"]
build-backend = "pdm.pep517.api"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pdm.dev-dependencies]
linters = [
    "isort",
    "black",
    "pylint>=3.0.0a5",
    "watchdog>=2.1.9",
]

[tool.pdm.scripts]
linter = "python scripts/linter.py"
test = "python tests/helloworld.py"

